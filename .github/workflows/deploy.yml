name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  APP_PATH: /opt/manutencao-app

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known_hosts
        run: ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Pull latest on server
        run: |
          ssh -o ServerAliveInterval=30 \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "cd ${{ env.APP_PATH }} && git fetch origin main && git reset --hard origin/main"

      - name: Build and restart
        run: |
          ssh -o ServerAliveInterval=30 -o ServerAliveCountMax=120 \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "cd ${{ env.APP_PATH }} && docker compose build --no-cache --pull manutencao-app manutencao-api && docker compose up -d --force-recreate --remove-orphans"

      - name: Remove containers conflitantes (Coolify legado + /opt/manutencaox lixo)
        run: |
          ssh -o ServerAliveInterval=30 ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'bash -s' <<"REMOTE"
          set -e
          # Coolify legado (nao usado mais)
          for c in $(docker ps -a --format '{{.Names}}' | grep -E '^vc84okoc0gkcg08wkskksg4o' || true); do
            echo "Removendo Coolify: $c"
            docker rm -f "$c" || true
          done
          # Stack manutencaox-* (lixo conforme CLAUDE.md)
          if [ -d /opt/manutencaox ]; then
            cd /opt/manutencaox && docker compose down -v --remove-orphans || true
          fi
          for c in manutencaox-app manutencaox-api manutencaox-backup manutencaox-db; do
            docker rm -f "$c" 2>/dev/null || true
          done
          echo "=== Containers restantes com rule manutencaox.com.br ==="
          for c in $(docker ps --format '{{.Names}}'); do
            labels=$(docker inspect "$c" --format '{{json .Config.Labels}}' 2>/dev/null)
            if echo "$labels" | grep -q manutencaox.com.br; then echo "--> $c"; fi
          done
          REMOTE

      - name: Diag erros
        run: |
          sleep 15
          TOKEN=$(curl -s -X POST https://manutencaox.com.br/api/auth/login -H "Content-Type: application/json" -d '{"email":"eduardodominikus@hotmail.com","senha":"123456"}' | grep -oE '"token":"[^"]+"' | sed 's/"token":"//;s/"$//')
          curl -s -H "Authorization: Bearer $TOKEN" https://manutencaox.com.br/api/relatorios/resumo >/dev/null
          curl -s -H "Authorization: Bearer $TOKEN" https://manutencaox.com.br/api/contratos >/dev/null
          curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"condominioId":"e284df7b-6885-4c17-a4db-7058573e6be6","titulo":"X","mensagem":"y"}' https://manutencaox.com.br/api/comunicados >/dev/null
          curl -s -X POST -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"condominioId":"e284df7b-6885-4c17-a4db-7058573e6be6","titulo":"T","recorrencia":"diaria"}' https://manutencaox.com.br/api/tarefas >/dev/null
          sleep 2
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'docker logs --since 30s manutencao-api 2>&1 | grep -E "ERROR|error]" | tail -30'

      - name: Health check
        run: |
          sleep 10
          ssh -o ServerAliveInterval=30 \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "docker ps --filter name=manutencao --format '{{.Names}}: {{.Status}}'"
